import * as React from "react";
import * as ReactDOM from "react-dom";
import {App} from "./main/app"


ReactDOM.render(
<App compiler="no importa" framework="no importa"/>,
  document.getElementById("root")
);
